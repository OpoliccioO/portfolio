import React from "react";
import StyledTagContainer from "../styles/StyledTagContainer";

const TagContainer = ({ children }) => {
  return <StyledTagContainer>{children}</StyledTagContainer>;
};

export default TagContainer;
