import styled from "styled-components";
import ComputerPlants from "../../assets/computer_plants.jpg";

const StyledHeader = styled.header`
  position: relative;
  background: url(${ComputerPlants}) no-repeat center center/cover;
`;

export default StyledHeader;
